name: _

environment:
  sdk: ^3.8.0

workspace:
  - packages/dart_bolt
  - packages/dart_neo4j
  - packages/dart_neo4j_ogm
  - packages/dart_neo4j_ogm_generator
  - packages/dart_packstream
dependencies:
  analyzer: ^8.0.0 # Configured via catalog
  build: ^4.0.0 # Configured via catalog
  source_gen: ^4.0.0 # Configured via catalog
